services:
  gamwich:
    image: dukerupert/gamwich:latest
    ports:
      - "127.0.0.1:8082:8080"
    volumes:
      - gamwich-data:/data
    environment:
      GAMWICH_DB_PATH: /data/gamwich.db
    restart: unless-stopped

volumes:
  gamwich-data:
